# Gravity - User Flow Diagram

## Installation & Setup Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER INSTALLATION                            │
└─────────────────────────────────────────────────────────────────┘

Step 1: Install Package
┌──────────────────────────────────────────────────────────────┐
│ $ npm install gravity-core                                  │
│                                                              │
│ ✅ Package installed                                         │
│ ✅ MCP server ready                                          │
│ ✅ Core library available                                   │
└──────────────────────────────────────────────────────────────┘
                            ↓

Step 2: Load Extension & Setup Native Host
┌──────────────────────────────────────────────────────────────┐
│ 1. Go to chrome://extensions                                 │
│ 2. Enable "Developer mode"                                   │
│ 3. Click "Load unpacked"                                     │
│ 4. Select extension/ folder                                  │
│ 5. Run: npx gravity-core setup-native-host                  │
│                                                              │
│ ✅ Extension loaded                                          │
│ ✅ Native host registered                                    │
│ ✅ Ready to connect to tabs                                 │
└──────────────────────────────────────────────────────────────┘
                            ↓

Step 3: Connect to Tab
┌──────────────────────────────────────────────────────────────┐
│ 1. Open website in Chrome                                    │
│ 2. Click extension icon                                      │
│ 3. Click "Connect to Tab"                                    │
│                                                              │
│ ✅ Extension attached to tab                                │
│ ✅ Chrome DevTools Protocol enabled                         │
│ ✅ Status: 🟢 GREEN (Connected)                             │
└──────────────────────────────────────────────────────────────┘
                            ↓

Step 4: Configure IDE
┌──────────────────────────────────────────────────────────────┐
│ Add to IDE's MCP config:                                     │
│                                                              │
│ {                                                            │
│   "mcpServers": {                                            │
│     "gravity": {                                             │
│       "command": "npx",                                      │
│       "args": ["gravity-core"],                             │
│       "disabled": false                                      │
│     }                                                        │
│   }                                                          │
│ }                                                            │
│                                                              │
│ ✅ IDE configured                                            │
│ ✅ MCP server will start when needed                        │
│ ✅ Ready to diagnose                                         │
└──────────────────────────────────────────────────────────────┘
                            ↓

Step 5: Start Diagnosing
┌──────────────────────────────────────────────────────────────┐
│ Ask your AI:                                                 │
│ "Diagnose the #modal element"                               │
│                                                              │
│ ✅ Ready to diagnose layouts!                               │
└──────────────────────────────────────────────────────────────┘
```

## Runtime Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    RUNTIME FLOW                                 │
└─────────────────────────────────────────────────────────────────┘

User asks AI:
"Diagnose the #modal element"
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ IDE (VSCode, Cursor, Kiro, etc.)                             │
│                                                              │
│ Sends request to MCP server via stdio                        │
└──────────────────────────────────────────────────────────────┘
        │
        │ MCP Protocol (stdio)
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ gravity-core (MCP Server)                                   │
│                                                              │
│ 1. Receives request                                          │
│ 2. Validates selector "#modal"                              │
│ 3. Connects to native host via WebSocket (port 9224)        │
└──────────────────────────────────────────────────────────────┘
        │
        │ WebSocket (port 9224)
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ Native Host Bridge (native-host.js)                          │
│                                                              │
│ 1. Receives WebSocket connection                             │
│ 2. Forwards CDP command via Native Messaging                │
└──────────────────────────────────────────────────────────────┘
        │
        │ Native Messaging (stdio)
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ Chrome Extension (background.js)                             │
│                                                              │
│ 1. Receives Native Messaging message                         │
│ 2. Receives CDP command:                                     │
│    {                                                         │
│      "type": "cdp_request",                                  │
│      "method": "DOM.querySelector",                          │
│      "params": { "selector": "#modal" }                      │
│    }                                                         │
└──────────────────────────────────────────────────────────────┘
        │
        │ Chrome DevTools Protocol
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ Browser Tab                                                  │
│                                                              │
│ 1. Executes CDP command                                      │
│ 2. Queries DOM for #modal                                    │
│ 3. Gets box model, styles, viewport metrics                 │
│ 4. Returns data                                              │
└──────────────────────────────────────────────────────────────┘
        │
        │ (reverse path)
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ Chrome Extension (background.js)                             │
│                                                              │
│ 1. Receives browser data                                     │
│ 2. Sends response via Native Messaging                       │
└──────────────────────────────────────────────────────────────┘
        │
        │ Native Messaging
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ Native Host Bridge (native-host.js)                          │
│                                                              │
│ 1. Receives Native Messaging message                         │
│ 2. Forwards via WebSocket:                                   │
│    {                                                         │
│      "type": "cdp_response",                                 │
│      "result": { "nodeId": 42, ... }                         │
│    }                                                         │
└──────────────────────────────────────────────────────────────┘
        │
        │ WebSocket
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ gravity-core (MCP Server)                                   │
│                                                              │
│ 1. Receives browser data                                     │
│ 2. Analyzes layout:                                          │
│    - Checks visibility (display, opacity, etc.)             │
│    - Checks offscreen (left, right, top, bottom)            │
│    - Checks z-index and positioning                         │
│    - Checks overflow                                        │
│ 3. Generates diagnostic report                              │
│ 4. Sends result via stdio                                    │
└──────────────────────────────────────────────────────────────┘
        │
        │ MCP Protocol (stdio)
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ IDE (VSCode, Cursor, Kiro, etc.)                             │
│                                                              │
│ Receives diagnostic result:                                  │
│ {                                                            │
│   "element": "#modal",                                       │
│   "issues": [                                                │
│     {                                                        │
│       "type": "offscreen-right",                             │
│       "severity": "high",                                    │
│       "message": "Element extends 50px beyond right edge",   │
│       "suggestion": "Add max-width: 100%"                    │
│     }                                                        │
│   ],                                                         │
│   "position": { ... },                                       │
│   "viewport": { ... }                                        │
│ }                                                            │
└──────────────────────────────────────────────────────────────┘
        │
        ▼
User sees results and fixes the issue!
```

## Connection States

```
┌─────────────────────────────────────────────────────────────────┐
│                    CONNECTION STATES                            │
└─────────────────────────────────────────────────────────────────┘

Extension Status:
┌──────────────────────────────────────────────────────────────┐
│ 🔴 RED (Disconnected)                                        │
│ - Extension loaded but not connected to tab                  │
│ - User needs to click "Connect to Tab"                       │
└──────────────────────────────────────────────────────────────┘
        │
        │ User clicks "Connect to Tab"
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ 🟡 YELLOW (Connecting)                                       │
│ - Extension attaching Chrome Debugger API                    │
│ - Enabling CDP domains                                       │
│ - Initializing connection                                    │
└──────────────────────────────────────────────────────────────┘
        │
        │ Connection established
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ 🟢 GREEN (Connected)                                         │
│ - Extension connected to tab                                 │
│ - Native host bridge running                                 │
│ - WebSocket server listening on port 9224                    │
│ - Ready to receive CDP commands                              │
│ - MCP server can connect and diagnose                        │
└──────────────────────────────────────────────────────────────┘
        │
        │ User closes tab or clicks disconnect
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│ 🔴 RED (Disconnected)                                        │
│ - Back to initial state                                      │
│ - User can connect to another tab                            │
└──────────────────────────────────────────────────────────────┘
```

## IDE Integration Examples

### VSCode
```
VSCode
  ↓
MCP Config: gravity
  ↓
Starts: npx gravity-core
  ↓
User asks AI: "Diagnose #modal"
  ↓
MCP server connects to extension
  ↓
Results shown in VSCode
```

### Cursor
```
Cursor
  ↓
MCP Config: gravity
  ↓
Starts: npx gravity-core
  ↓
User asks AI: "Diagnose #modal"
  ↓
MCP server connects to extension
  ↓
Results shown in Cursor
```

### Kiro
```
Kiro
  ↓
MCP Config: gravity
  ↓
Starts: npx gravity-core
  ↓
User asks AI: "Diagnose #modal"
  ↓
MCP server connects to extension
  ↓
Results shown in Kiro
```

## Key Points

✅ **Native Host Bridge** - Bridges WebSocket (MCP) and Native Messaging (Extension)
✅ **No manual setup** - Extension auto-registers with CLI
✅ **No terminal needed** - Everything happens in the background
✅ **No port conflicts** - Native host manages port 9224
✅ **Works everywhere** - Any IDE with MCP support
✅ **Real-time** - Instant diagnostics
✅ **Secure** - All data stays local
